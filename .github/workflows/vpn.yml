name: Merge VLESS Configs

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  merge:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Fetch and merge VLESS configs
        run: |
          SOURCES=(
            "https://raw.githubusercontent.com/igareck/vpn-configs-for-russia/refs/heads/main/Vless-Reality-White-Lists-Rus-Mobile-2.txt"
            "https://raw.githubusercontent.com/igareck/vpn-configs-for-russia/refs/heads/main/Vless-Reality-White-Lists-Rus-Mobile.txt"
            "https://raw.githubusercontent.com/igareck/vpn-configs-for-russia/refs/heads/main/WHITE-CIDR-RU-all.txt"
            "https://raw.githubusercontent.com/igareck/vpn-configs-for-russia/refs/heads/main/WHITE-CIDR-RU-checked.txt"
            "https://raw.githubusercontent.com/igareck/vpn-configs-for-russia/refs/heads/main/WHITE-SNI-RU-all.txt"
            "https://raw.githubusercontent.com/igareck/vpn-configs-for-russia/refs/heads/main/BLACK_SS%2BAll_RUS.txt"
            "https://raw.githubusercontent.com/igareck/vpn-configs-for-russia/refs/heads/main/BLACK_VLESS_RUS.txt"
            "https://raw.githubusercontent.com/igareck/vpn-configs-for-russia/refs/heads/main/BLACK_VLESS_RUS_mobile.txt"
          )
          
          > raw-vless.txt
          
          for URL in "${SOURCES[@]}"; do
            curl -fsSL "$URL" | grep -E '^vless://' >> raw-vless.txt || true
          done
          
          sort -u raw-vless.txt > dedup-vless.txt
          
          > merged-vless.txt
          COUNTER=1
          while IFS= read -r line; do
            BASE=$(echo "$line" | sed 's/#.*//')
            echo "${BASE}#🇪🇺LTE | 1.${COUNTER}" >> merged-vless.txt
            COUNTER=$((COUNTER + 1))
          done < dedup-vless.txt
          
          echo "Всего конфигов: $(wc -l < merged-vless.txt)"

      - name: Generate subscription file
        run: |
          base64 -w 0 merged-vless.txt > vlees.txt
          echo "Файл подписки готов"

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add merged-vless.txt vlees.txt
          git diff --staged --quiet || git commit -m "Auto: обновление VLESS конфигов $(date -u '+%Y-%m-%d %H:%M UTC')"
          git push
