name: Merge VLESS Configs

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

env:
  REPO_RAW: "https://raw.githubusercontent.com/${{ github.repository }}/main/configs"

jobs:
  merge:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install dependencies
        run: sudo apt-get install -y jq curl

      - name: Build configs
        run: |
          python3 << 'PYEOF'
          import os, json, re, urllib.request

          GROUP_SIZE = 20
          OUTPUT_DIR = "configs"
          VLESS_TXT  = "vlees.txt"
          os.makedirs(OUTPUT_DIR, exist_ok=True)

          SOURCES = [
            "https://raw.githubusercontent.com/igareck/vpn-configs-for-russia/refs/heads/main/Vless-Reality-White-Lists-Rus-Mobile-2.txt",
            "https://raw.githubusercontent.com/igareck/vpn-configs-for-russia/refs/heads/main/Vless-Reality-White-Lists-Rus-Mobile.txt",
            "https://raw.githubusercontent.com/igareck/vpn-configs-for-russia/refs/heads/main/WHITE-CIDR-RU-all.txt",
            "https://raw.githubusercontent.com/igareck/vpn-configs-for-russia/refs/heads/main/WHITE-CIDR-RU-checked.txt",
            "https://raw.githubusercontent.com/igareck/vpn-configs-for-russia/refs/heads/main/WHITE-SNI-RU-all.txt",
            "https://raw.githubusercontent.com/igareck/vpn-configs-for-russia/refs/heads/main/BLACK_SS%2BAll_RUS.txt",
            "https://raw.githubusercontent.com/igareck/vpn-configs-for-russia/refs/heads/main/BLACK_VLESS_RUS.txt",
            "https://raw.githubusercontent.com/igareck/vpn-configs-for-russia/refs/heads/main/BLACK_VLESS_RUS_mobile.txt",
          ]

          def parse_vless(uri):
            try:
              body  = uri[len("vless://"):]
              uuid  = body.split("@")[0]
              rest  = body.split("@", 1)[1]
              hostport = rest.split("?")[0]
              host  = hostport.rsplit(":", 1)[0]
              port  = hostport.rsplit(":", 1)[1]
              params_raw = rest.split("?", 1)[1].split("#")[0] if "?" in rest else ""
              params = dict(p.split("=", 1) for p in params_raw.split("&") if "=" in p)
              return {
                "uuid": uuid,
                "host": host,
                "port": int(port),
                "flow": params.get("flow", "xtls-rprx-vision"),
                "sni":  params.get("sni", host),
                "pbk":  params.get("pbk", ""),
                "fp":   params.get("fp", "chrome"),
                "sid":  params.get("sid", ""),
                "net":  params.get("type", "tcp"),
              }
            except Exception:
              return None

          def make_outbound(tag, p):
            return {
              "tag": tag, "protocol": "vless",
              "settings": {"vnext": [{"address": p["host"], "port": p["port"],
                "users": [{"id": p["uuid"], "encryption": "none", "flow": p["flow"]}]}]},
              "streamSettings": {"network": p["net"], "tcpSettings": {}, "security": "reality",
                "realitySettings": {"serverName": p["sni"], "publicKey": p["pbk"],
                  "shortId": p["sid"], "fingerprint": p["fp"]}}
            }

          def make_config(label, outbounds, tags):
            bal_tag = "AUTO-BAL"
            return {
              "remarks": label,
              "dns": {"servers": ["1.1.1.1", "1.0.0.1"], "queryStrategy": "UseIP"},
              "routing": {
                "domainMatcher": "hybrid", "domainStrategy": "IPIfNonMatch",
                "rules": [
                  {"type": "field", "protocol": ["bittorrent"], "outboundTag": "direct"},
                  {"type": "field", "inboundTag": ["socks", "http"], "balancerTag": bal_tag}
                ],
                "balancers": [{"tag": bal_tag, "selector": tags,
                  "strategy": {"type": "leastPing",
                    "settings": {"maxRTT": "2000ms", "expected": 3, "baselines": ["1200ms"], "tolerance": 0.1}},
                  "fallbackTag": "direct"}]
              },
              "inbounds": [
                {"tag": "socks", "port": 10808, "listen": "127.0.0.1", "protocol": "socks",
                 "settings": {"udp": True, "auth": "noauth"},
                 "sniffing": {"enabled": True, "routeOnly": False, "destOverride": ["http","tls","quic"]}},
                {"tag": "http", "port": 10809, "listen": "127.0.0.1", "protocol": "http",
                 "settings": {"allowTransparent": False},
                 "sniffing": {"enabled": True, "routeOnly": False, "destOverride": ["http","tls","quic"]}}
              ],
              "outbounds": outbounds + [
                {"tag": "direct", "protocol": "freedom"},
                {"tag": "block",  "protocol": "blackhole"}
              ],
              "burstObservatory": {
                "pingConfig": {"timeout": "2s", "interval": "10s", "sampling": 2,
                  "httpMethod": "GET", "destination": "https://www.gstatic.com/generate_204", "connectivity": ""},
                "subjectSelector": tags
              }
            }

          # vlees.txt header
          with open(VLESS_TXT, "w") as f:
            f.write("#profile-title: HiBypass | LTE FREE\n")
            f.write("#profile-update-interval: 6\n")
            f.write("#support-url: https://t.me/Hibypass\n")
            f.write("#traffic-limit: inf\n")
            f.write("#traffic-remaining: inf\n")

          generated_files = []  # список сгенерированных json файлов

          for block_idx, url in enumerate(SOURCES, 1):
            print(f"--- Block {block_idx}: {url}")
            try:
              with urllib.request.urlopen(url, timeout=30) as r:
                lines = r.read().decode("utf-8", errors="ignore").splitlines()
            except Exception as e:
              print(f"    ERROR: {e}")
              continue

            vless_lines = sorted(set(l.strip() for l in lines if l.strip().startswith("vless://")))
            print(f"    found: {len(vless_lines)}")
            if not vless_lines:
              continue

            chunks = [vless_lines[i:i+GROUP_SIZE] for i in range(0, len(vless_lines), GROUP_SIZE)]

            for group_idx, chunk in enumerate(chunks, 1):
              label = f"LTE | Auto {block_idx}.{group_idx}"
              outbounds, tags = [], []

              for node_idx, uri in enumerate(chunk, 1):
                p = parse_vless(uri)
                if not p or not p["pbk"]:
                  continue
                tag = f"n{block_idx}g{group_idx}-{node_idx}"
                outbounds.append(make_outbound(tag, p))
                tags.append(tag)

              if not tags:
                continue

              cfg = make_config(label, outbounds, tags)
              fname = f"lte_{block_idx}_{group_idx}.json"
              fpath = os.path.join(OUTPUT_DIR, fname)
              with open(fpath, "w") as f:
                json.dump(cfg, f, ensure_ascii=False, indent=2)

              generated_files.append(fname)

              with open(VLESS_TXT, "a") as f:
                for uri in chunk:
                  base = uri.split("#")[0]
                  f.write(f"{base}#{label}\n")

              print(f"    OK {fpath} ({len(tags)} nodes)")

          print(f"\nTotal JSON files: {len(generated_files)}")

          # сохраняем список файлов для следующего шага
          with open("/tmp/generated_files.txt", "w") as f:
            for fname in generated_files:
              f.write(fname + "\n")

          PYEOF

      - name: Build subscription index
        run: |
          REPO_RAW="https://raw.githubusercontent.com/${{ github.repository }}/main/configs"

          # subscription.txt — список raw-ссылок на каждый lte_X_Y.json
          # Hiddify/v2rayTun могут импортировать его как набор конфигов
          {
            echo "#profile-title: HiBypass | LTE ALL CONFIGS"
            echo "#profile-update-interval: 6"
            echo "#support-url: https://t.me/Hibypass"
            while IFS= read -r fname; do
              echo "${REPO_RAW}/${fname}"
            done < /tmp/generated_files.txt
          } > configs/subscription.txt

          echo "--- subscription.txt ---"
          cat configs/subscription.txt

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add configs/ vlees.txt
          git diff --staged --quiet || git commit -m "Auto: update $(date -u '+%Y-%m-%d %H:%M UTC')"
          git push
